{
  "name": "@cawtooth-examples/imf",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser demo: load an IMF / WLF file and play it through the OPL worklet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cawtooth": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
