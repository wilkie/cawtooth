{
  "name": "@cawtooth-examples/sid-tone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal browser demo: programs a SID voice and plays a sustained tone via the reSID-backed AudioWorklet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cawtooth": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
