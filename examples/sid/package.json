{
  "name": "@cawtooth-examples/sid",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PSID playback demo — loads a .sid file and plays it through the sidplay AudioWorklet (fake6502 + reSID).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cawtooth": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
